# 정적 파일 서빙 + SPA fallback. /api 프록시는 Ingress가 담당.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
